# pi_cam_loader.py — pip deps (use with venv --system-site-packages for libcamera)
opencv-python
picamera2

# stream_server.py (FastAPI + WebSocket)
fastapi
uvicorn[standard]

# webrtc_handler.py (WebRTC streaming)
aiortc
av
